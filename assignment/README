The files contained in ~/CC200/assignment include: the main source files; assignment.c and assignment.h, 
a topology file containing the nodes from the assignment specification, a folder containing some example 
outputs and this readme file. 

All the code required to run the assignment is located in assignment.c and assignment.h.

1. To run the assignment, make sure you are in the assignment directory and run the command: cnet TOPOLOGY.
2. Once the cnet windows open, target the topology window and hit space bar.